# WeddingWin release test matrix

Status: **OPEN — this matrix is the submission gate, not a record of a completed App Store release.**

Prepared: 2026-08-28
Target: WeddingWin `1.0.0`, bundle ID `ca.weddingwin.app`

Use this matrix against one immutable release commit/tag, one deployed backend revision, and the processed TestFlight build created from them. For every passing row, record the tester, UTC date, device/OS, build number, backend revision, and evidence path or App Store/EAS build URL in the release ticket. Never put passwords, tokens, Apple keys, service-role keys, certificates, or provisioning profiles in this file.

## Status meanings

| Status | Meaning |
| --- | --- |
| Configured | Present in source, but still requires release-build verification. |
| Working-tree pass | Passed against the local Release source/backend now recorded by `v1.0.0-rc.2`; repeat from a clean tag checkout and TestFlight where required. |
| Working-tree partial | The stated subset passed locally; the remaining scope in the evidence column is still required. |
| Prior snapshot only | Observed on an earlier Simulator preparation build; not proof for the final tagged source. |
| Pending | Required test has not been recorded against the final release. |
| Blocked | Cannot run safely or truthfully until the named prerequisite is resolved. |
| Pass | Evidence has been recorded against the exact tagged/TestFlight release. |
| Fail | Defect found; fix, rebuild/redeploy, and repeat before submission. |

## Release evidence record

Final distribution and owner-approval fields remain pending until evidence is recorded in the private release ticket. Working-tree Simulator evidence is summarized below without credentials or private user data.

| Evidence | Current value |
| --- | --- |
| Release commit and tag | The tested source is committed and recorded by local tag `v1.0.0-rc.2`. Nothing is pushed, signed, or represented in TestFlight. |
| Deployed Supabase/Brilliant Directories revision or checksums | Current live migration identifiers and all 24 active Edge Function versions are recorded in `SUPABASE_DEPLOYMENT_PROVENANCE.md`. The latest migrations are `20260828221025` (`enable_private_app_reviewers`) and `20260828221307` (`scope_private_app_reviewer_pairs`); release-path versions include `bd-email-login` v34, `apple-native-login` v14, `google-oauth-callback` v14, `bd-delete-account` v7, `bd-push-sweep` v8, `bd-chat-sync` v38, `bd-chat-status` v26, and `bd-qr-bingo-sync` v13. Record immutable source/deployment checksums privately. |
| EAS build ID/URL and App Store Connect build number | `<PENDING>` |
| Processed TestFlight build install | `<PENDING>` |
| Physical iPhone tester/date/device/iOS | `<PENDING>` |
| Physical iPad tester/date/device/iPadOS | `<PENDING>` |
| Working-tree Simulator evidence | `The fresh current-source Release build completed in 38.2 seconds and launched on iPhone 17 Pro Max Simulator, iOS 26.5: reviewer email login, explicit one-time dashboard bridge, in-app vendor dashboard with visibly embedded Vimeo instead of a Safari escape, QR scanner, blocked chat, controlled reviewer app↔website text, About/deletion confirmation and cancel, relaunch, and installed icon. The generated Release Info.plist was inspected and contained only the native QR-camera usage description, with no photo-library, microphone, or background-mode declaration. This is not a signed archive or physical-device result.` |
| Disposable deletion test record | `Fresh disposable members 38978 and 38979 passed the revised two-participant test: both message directions delivered, deleting 38978 removed account-owned data while preserving 38979's read-only history, closed the thread, rejected new sends with 423, and final cleanup removed both test accounts. Physical Apple-linked and uploaded-build repeats remain pending.` |
| Working screenshot inventory | `2 clean primary Pro Max 1320×2868 + 9 iPhone 6.3-inch QA 1206×2622 + 1 clean iPad 2064×2752 under assets/app-store/screenshots` |
| Final policy version/effective date and public URLs | `<PENDING>` |
| Owner/legal approvals | `<PENDING>` |

## A. Configuration, signing, and backend gates

| ID | Gate / test | Environment | Current status | Required pass evidence |
| --- | --- | --- | --- | --- |
| CFG-01 | App identity and metadata | Source inspection | Configured | `app.json` shows name/slug, version `1.0.0`, iOS build number `1`, bundle ID `ca.weddingwin.app`, icon, portrait, iPad support, Sign in with Apple, the QR-camera purpose string, no native photo-library purpose string, and `ITSAppUsesNonExemptEncryption: false`; owner/legal approves encryption answer. |
| CFG-02 | EAS release configuration | Source + Expo owner account | Blocked | Link the existing `eas.json` to the organization-owned Expo project, add `extra.eas.projectId` and `submit.production.ios.ascAppId`, and record the actual auto-incremented build number. These identifiers are not yet present in the current source. |
| CFG-03 | Production Google OAuth | Server provider config + TestFlight | Working-tree partial / physical test blocked | PKCE, signed state, one-time native exchange, browser binding, expiry, missing/mismatched-cookie rejection, and replay rejection are deployed and controlled backend tests passed. Real Google first/returning/cancel/error flows still require a physical iPhone/TestFlight build. The unused blank `extra.googleOAuth.iosClientId` has been removed. |
| CFG-04 | Apple distribution signing | Apple Developer + EAS/Xcode | Blocked | Owner authentication/2FA complete; explicit App ID has required capabilities; Apple Distribution certificate and App Store profile are valid; archive validation passes. No Apple credentials were available in this preparation environment. |
| CFG-05 | Production push signing/configuration | Apple Developer + EAS + Supabase | Working-tree partial / physical test blocked | Central sweep delivery, Expo ticket/receipt tracking, durable bounded retry with `Retry-After`, finite missing-receipt expiry, and `DeviceNotRegistered` disablement are deployed; duplicate sending was removed from chat status. The generated local Release Info.plist contains no background-mode declaration. APNs signing, Expo project linkage, signed Info.plist/entitlement inspection, and foreground/background/terminated delivery still require the signed physical build. |
| CFG-06 | Exact backend deployment | Supabase + Brilliant Directories | Working-tree pass | One-time auth exchanges, deletion preservation, push delivery tracking, same-sync website close flushing, WeddingWin.ca `/app-login` code redemption, service-only expiring private-reviewer pair access, and idempotent controlled QR replay are live. Reviewer access is restricted to vendor `38970` and couple `38971` while the vendor remains nonpublic; `bd-chat-sync` v38, `bd-chat-status` v26, and JWT-verified `bd-qr-bingo-sync` v13 are deployed. Native login plus explicit bridge/redeem returned 200; the code was single-use and both exchange tables were empty afterward. Controlled reviewer app→website and active-couple website→private-vendor-app text also passed. Record checksums/environment flags and repeat from TestFlight before final Pass. |
| CFG-07 | Dependency reproducibility | Clean release checkout | Working-tree partial | Expo dependency check, typecheck, lint, 22 Edge Function checks, **60/60 shared Deno tests**, the focused WebView URL-policy suite **7/7**, and the 38.2-second Release Simulator build pass. `npm audit --omit=dev` reports 8 high and 0 critical/moderate/low findings, all through Expo/Metro's `image-size` tooling chain; the offered automatic fix is a breaking Expo 57 upgrade. Repeat clean install/doctor/audit against the final tag and do not force-upgrade the release candidate. |
| CFG-08 | Secrets and reviewer credentials | Git + release systems | Working-tree partial | Reviewer/private deployment values remain excluded from tracked release material and the source scan passes. `APP_EMAIL_CHANGE_SECRET` still requires an explicitly approved coordinated rotation: the replacement is absent from live Supabase and BD widget `329` retains the prior hardcoded value. Never place either value in Git or release evidence. |
| CFG-09 | Privacy/network inventory | Final TestFlight + production WebViews | Pending | Actual website scripts, cookies, analytics, ads, payment, captcha, CDN, email, logs, retention, and recipients match `APP_PRIVACY_ANSWERS.md` and the published policy. |
| CFG-10 | No-marketing raffle posture | Deployed app/backend + contracts | Blocked | Entry notice, official rules, vendor agreement, absence of entrant-list exports, selected-potential-winner disclosure, suppressed email, campaigns, and staff procedure limit data to named-draw administration and prize fulfilment. Entry does not subscribe entrants to marketing. |
| CFG-11 | WebView navigation/cookie hardening | Exact tagged TestFlight build + network log | Working-tree pass | The focused URL-policy suite passes **7/7** and the fresh Release Simulator vendor dashboard stayed inside WeddingWin with its Vimeo iframe visibly embedded instead of opening Safari. Top-frame behavior is unchanged: trusted WeddingWin/WedWebsite HTTPS remains in-app, unrelated HTTPS uses the system-browser path, and deceptive/insecure/active-content destinations fail closed. Non-top-frame requests may remain embedded only for HTTPS and `about:blank`; `http:`, `javascript:`, `data:`, and `file:` subframes are blocked. Signed-in Google/OAuth starts remain blocked, all navigation reaches the centralized policy, and third-party cookies remain disabled. Repeat against TestFlight, network logs, redirects/pop-ups, and the approved external-link inventory. |

## B. Reviewer accounts, QR, and controlled data

| ID | Gate / test | Environment | Current status | Required pass evidence |
| --- | --- | --- | --- | --- |
| FIX-01 | Private couple reviewer account `38971` | Clean TestFlight install + signed-out web | Configured / working-tree iPhone and iPad login pass | Controlled couple account, displayed as `App Review`, is assigned to isolated event `app-review-weddingwin-2026-38970`. Final Release email login/native-session creation passed on iPhone 17 Pro Max; prior Release login/menu passed on 13-inch iPad Pro (M5). Verify reset state and the exact uploaded build immediately before submission. |
| FIX-02 | Private vendor reviewer account `38970` | Clean TestFlight install + signed-out web | Configured / working-tree native chat and dashboard pass | Private/nonpublic vendor is assigned to the isolated review event and its login has been prepared. Service-only, expiring access is live only for the exact vendor `38970` ↔ couple `38971` reviewer pair, without publishing the vendor listing. Controlled native app→website text and the supported active-couple website→private-vendor-app reply passed. The local Release dashboard remained in-app and visibly embedded Vimeo. Before final Pass, verify draw settings, signed-out directory/profile/search/sitemap/public-event/marketing absence, and the exact uploaded build. |
| FIX-03 | Reviewer credentials handling | Password manager + App Store Connect | Pending | Passwords work immediately before submission, are pasted only into App Store Connect credential fields, and are not in Git, notes attachments, screenshots, email, or logs. |
| FIX-04 | Normal text-chat fixture | App + website | Working-tree pass | A fresh controlled conversation was recorded in both clients with fictional text: app→website was visible in the authenticated Chrome thread; the supported active-couple website→private-vendor-app reply persisted after reload, mirrored through the database/API, and appeared as an incoming native Simulator bubble. The inactive vendor's website send was correctly blocked by Brilliant Directories and was not treated as delivered. Native image sending remains intentionally disabled. Repeat from TestFlight before final Pass. |
| FIX-05 | Disposable report/block fixture | App + website | Working-tree pass / documented limitation | App-to-website and website-to-app controlled messages were verified, report/block closed the conversation on both clients, and the final Release app preserved history while exposing no composer. Same-sync replacement-thread close is deployed and regression-tested. A fresh BD-only thread may exist until the next app sync discovers and closes it; this is not preventive website-wide blocking. |
| FIX-06 | Disposable deletion accounts | App + backend stores | Working-tree pass | Fresh members 38978/38979 passed the deployed preservation behavior: account-owned data and login were removed, shared history remained read-only for the survivor, the thread closed, and sends were rejected. A physical Apple-linked account, approved retention period, provider/backup checks, and exact uploaded-build repeat remain pending. |
| FIX-07 | Vendor `38970` sample QR | Isolated event + printed image + physical iPhone | Configured | `assets/app-store/sample-qr-review-vendor-38970.png` targets isolated event `app-review-weddingwin-2026-38970`. Before Pass, reset couple `38971`, verify printed scan, progress, duplicate handling, and absence of production-state changes. |
| FIX-08 | Review fixture isolation | Backend + signed-out web | Pending | Vendor `38970`, couple `38971`, scans, entries, selections, and prize state remain isolated; vendor remains nonpublic; no production entrant or event state is read or changed; review reset is repeatable. |
| FIX-09 | Isolated review draw | App + website + backend | Configured / legal approval pending | Test event/prize/rules and email suppression are deployed. Before Pass, verify eligibility, current-rules consent, alternate free-entry instructions, draw timing/selection limit, potential-winner verification, reset, and no-marketing controls with fictional records only. Production and reviewer-fixture email must remain suppressed. |

## C. Automated and Simulator regression

| ID | Flow | Environment | Current status | Required pass evidence |
| --- | --- | --- | --- | --- |
| SIM-01 | Clean iOS Release build and launch | iPhone/iPad Simulator | Working-tree pass | The fresh current-source build completed in **38.2 seconds** and launched in Release on iPhone 17 Pro Max Simulator, iOS 26.5. The installed icon, cold relaunch, production config, and logs were inspected; no QA QR payload or credential-bearing debug output was present. The generated local Release Info.plist exposes only the QR-camera usage description and no photo-library, microphone, or background-mode declaration; re-inspect the signed archive before upload. |
| SIM-02 | Role selection and email auth | iPhone Simulator | Working-tree partial | Final reviewer couple sign-out and real email login passed; the currently deployed `bd-email-login` v34 includes linked-email consistency and corrected first-attempt throttle hardening. The flow returned a native session without an orphan website exchange. Full vendor/wrong-password/error matrix remains for TestFlight. |
| SIM-03 | Couple and vendor navigation | iPhone Simulator | Working-tree partial | Final couple home, website dashboard, Back to app, native chat, QR, About, delete confirmation/cancel, and tab-bar suppression passed. The private vendor dashboard stayed in-app and visibly embedded its Vimeo player after the subframe fix. Vendor draw settings, broader external-link/redirect coverage, offline behavior, Dynamic Type, and iPad coverage remain. |
| SIM-04 | Chat app→website and website→app | Simulator + production-equivalent website | Working-tree pass | The fresh controlled reviewer round trip passed: app→website text appeared in the authenticated Chrome thread; the valid active-couple website→private-vendor-app reply survived website reload, mirrored through database/API, and rendered as an incoming native bubble. The inactive vendor website send was correctly rejected and not counted. The final Release blocked thread separately retained both clients' history and exposed no composer. Native image sending remains intentionally disabled. |
| SIM-05 | Report/member block | Simulator + website | Working-tree pass / known limitation | Durable report, website close, final app suppression, preserved history, and no-composer behavior passed; same-sync replacement-thread close is deployed and tested. A BD-only entry point may create a fresh thread before synchronization discovers it. |
| SIM-06 | QR URL emulation and backend replay | iPhone Simulator + live controlled backend | Working-tree pass | Canonical reviewer QR duplicate handling returned `Already scanned` without changing the scan timestamp. After hardening `bd-qr-bingo-sync` v13, a controlled direct replay returned 200 with `fixture: true`, kept the card/database count at 1→1, and preserved the original `scanned_at`. Dedicated replay plus release-policy tests pass for trusted, legacy, malformed, wrong-host, insecure, and active-content payloads. The final production scanner contains no simulation control. This does not replace printed-camera testing. |
| SIM-07 | Raffle eligibility/consent/time logic | iPhone Simulator + backend clock fixtures | Pending | Open/closed/not-started deadlines, timezone boundaries, duplicate entry, missing contact fields, rules version, consent text, vendor isolation, selection maximum, signature/replay, selected-potential-winner disclosure, and no-marketing behavior pass. Use fictional isolated data only. |
| SIM-08 | Native email-account deletion | Simulator + live review backend | Working-tree pass | Deployed two-account deletion preservation passed with members 38978/38979. The final Release confirmation/cancel UI was rechecked on iPhone 17 Pro Max. Physical Apple revocation, provider/backups, approved retention, error/fallback, and uploaded-build repeats remain. |
| SIM-09 | iPad layout smoke test | 13-inch iPad Pro (M5) Simulator | Working-tree partial | Release reviewer-couple login and portrait native menu passed; `ipad-01-couple-menu.png` is `2064×2752`. WebViews, keyboard/composer, modals, scanner, Dynamic Type, rotation/window behavior, VoiceOver, and full navigation remain pending. |
| SIM-10 | Logs and recovery | iPhone/iPad Simulator | Working-tree partial | Final runtime/OS logs contain no app crash, fatal exception, credential-bearing URL, token, password, or app-login secret. The generated local Release Info.plist contains no background-mode declaration; confirm the same in the signed build. The iOS 26.5 Simulator WebKit accessibility duplicate-class warning, offline/slow-network recovery, and physical recovery remain pending. |
| SIM-11 | Accessibility | iPhone/iPad Simulator + physical confirmation | Pending | Verify VoiceOver labels/hints, focus order, modal focus trapping/restoration, scanner and report/delete announcements, keyboard-only/switch paths where applicable, touch targets, contrast, Reduce Motion, and default/large accessibility Dynamic Type without clipping. Repeat critical flows on the exact uploaded build. |

## D. Physical iPhone and iPad TestFlight matrix

Do not replace these rows with Simulator or locally installed development-build results.

| ID | Flow | Required hardware/state | Current status | Required pass evidence |
| --- | --- | --- | --- | --- |
| DEV-01 | Fresh install, upgrade, reinstall | Supported iPhone / processed TestFlight build | Blocked | All three lifecycle paths preserve or clear state as designed; exact TestFlight build number recorded. |
| DEV-02 | Sign in with Apple | Physical iPhone, real Apple ID | Blocked | First login, Hide My Email, returning login, cancel/error, sign-out, relink, one-time website app-login exchange, and account-deletion revocation pass. Confirm the production backend accepts the WeddingWin bundle/service audiences and rejects Expo Go's `host.exp.Exponent`; `ALLOW_EXPO_GO_APPLE_AUD=1` is development-only and must be unset/disabled in production. |
| DEV-03 | Google login | Physical iPhone, production OAuth | Blocked | First/returning login, cancel/error, PKCE redirect/exchange back to app, expiry and replay rejection, sign-out, and relink pass without dev credentials or member/session credentials in callback URLs/logs. |
| DEV-04 | Printed QR camera scan | Physical iPhone + printed `38970` review sample | Blocked | Permission allow/deny/re-enable, normal/low light, focus/distance, one-scan behavior, duplicate/invalid code, isolated-event progress, and privacy logs pass. |
| DEV-05 | Chat push: foreground | Physical iPhone + controlled sender | Blocked | One generic notification/no private message text, correct unread/badge behavior, tap route, no duplicate, and one accepted Expo ticket recorded by the centralized sweep worker. |
| DEV-06 | Chat push: background | Physical iPhone + controlled sender | Blocked | Same checks with app backgrounded. |
| DEV-07 | Chat push: terminated | Physical iPhone + controlled sender | Blocked | Same checks after force-quit/termination; cold tap route and session behavior pass. |
| DEV-08 | Notification token lifecycle | Physical iPhone + backend inspection | Blocked | Deny/allow, reinstall/token rotation, multiple devices if supported, sign-out unregister, account-deletion purge, ticket rejection, receipt success/error, `DeviceNotRegistered` disablement, retry/no-duplicate behavior, and authorization controls pass through the centralized worker. |
| DEV-09 | Disabled photo attachment | Physical iPhone | Pending | No native image-send control or media permission prompt is exposed in the release build; existing/historical message media does not bypass the disabled gate. If image sending is re-enabled later, add a full photo-library permission/upload/deletion matrix before release. |
| DEV-10 | End-to-end account deletion | Physical iPhone + all production-like stores | Blocked | A fresh disposable two-participant email-account test passed the deployed preservation/redaction design. This physical row still requires disposable email and Apple accounts on the exact uploaded build; proof that account-owned data/login are removed; Apple revocation; provider/backup checks; closure/blocking of each related conversation; recipient-visible read-only shared history and moderation evidence; and owner/legal approval of a finite retention duration or criterion plus exceptions. |
| DEV-11 | Network and interruption recovery | Physical iPhone | Blocked | Airplane mode, slow/lost network, app background during request, duplicate tap, API timeout/rate limit, and recovery do not duplicate messages/entries/draw/deletion or expose secrets. |
| DEV-12 | iPad final pass | Physical supported iPad / TestFlight | Blocked | Full critical flow, permissions, keyboard/modals/WebViews, portrait layout, rotation/window behavior, memory/crash logs, and screenshot dimensions pass. |

## E. Distribution, screenshots, and submission

| ID | Gate / test | Environment | Current status | Required pass evidence |
| --- | --- | --- | --- | --- |
| DIST-01 | Store archive/build | EAS production or local Xcode archive | Blocked | Signed App Store IPA/archive succeeds; bundle/version/build/entitlements/icon/privacy manifests are inspected; no debug/test secret. |
| DIST-02 | Upload and processing | App Store Connect | Blocked | Upload succeeds, no unresolved validation warning/error, export answer matches binary, and processed build is selectable. |
| DIST-03 | Internal TestFlight acceptance | Physical iPhone/iPad | Blocked | All applicable DEV rows pass on the processed build, not an ad-hoc or Simulator build. |
| DIST-04 | Final screenshots | Exact release source + App Store Connect | Pending / two primary iPhone shots ready | Two visually verified credential-free iPhone 17 Pro Max RGB PNGs at `1320×2868` are designated as the primary 6.9-inch marketing shots. Nine accepted-size 6.3-inch files remain QA evidence, and one clean `2064×2752` iPad candidate exists. Final iPad/set selection, exact-release consistency review, upload, and App Store Connect preview inspection remain before Pass. |
| DIST-05 | App Privacy and public policy | App Store Connect + public website | Blocked | Published answers, policy, privacy choices, retention/deletion, no-marketing raffle posture, WebView inventory, and final binary privacy report agree. |
| DIST-06 | Reviewer notes/accounts/QR | App Store Connect + attachments | Blocked | Notes describe only verified behavior; private accounts `38971`/`38970` pass their documented flows; isolated `38970` sample QR is current; email suppression and reset are verified; support/reset contact is monitored. |
| DIST-07 | Commit, tag, and provenance | Git + release ticket | Working-tree partial | Current deployed-backend versions are recorded and the tested source is captured by local tag `v1.0.0-rc.2`. Separately push it and record source/backend checksums, signed archive, EAS/ASC build ID, and processed build number. The push, archive, and TestFlight build are not yet recorded. |
| DIST-08 | Owner/legal approvals | Release ticket | Blocked | Privacy, retention, contest sponsor/rules/territory/age/skill question, vendor agreement, no-marketing posture, payments/IAP, content rights, and export classification are approved. |

## Final release decision

Submission is **NO-GO** while any required row is `Blocked`, `Pending`, `Fail`, `Prior snapshot only`, `Working-tree partial`, or only a `Working-tree pass`. A Simulator build passing is necessary local evidence but never satisfies signing, TestFlight, production push, real federated login, printed-camera, physical iPad, or the remaining physical/Apple/provider deletion rows.
